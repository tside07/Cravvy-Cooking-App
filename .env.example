# Copy to .env at repo root (gitignored). Required for Flutter app startup.
#
# Supabase Dashboard → Project Settings → API
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=eyJ...paste_anon_key_here

# OAuth deep link (must match Supabase Redirect URLs + oauth_config.dart):
# com.example.cravvy_cooking_app://login-callback

# Import scripts use tools/.env with SERVICE_ROLE key — see tools/.env.example
