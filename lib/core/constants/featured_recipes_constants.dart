/// Home Explore Recipes teaser — subset size after filters.
class FeaturedRecipesConstants {
  FeaturedRecipesConstants._();

  static const int featuredRecipeCount = 8;
  static const int featuredRecipeCountMin = 6;
  static const int featuredRecipeCountMax = 10;
}

/// Shorthand for [FeaturedRecipesConstants.featuredRecipeCount].
const int kFeaturedRecipeCount = FeaturedRecipesConstants.featuredRecipeCount;
